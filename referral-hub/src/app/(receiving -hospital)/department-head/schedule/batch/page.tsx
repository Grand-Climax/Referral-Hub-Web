import BatchSchedulePage from '@/components/department-head/schedule/BatchSchedulePage';

export const metadata = {
  title: 'Run Batch Schedule – Department Head',
};

export default function BatchScheduleRoute() {
  return <BatchSchedulePage />;
}
