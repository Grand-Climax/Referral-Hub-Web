import CapacitySettingsPage from '@/components/department-head/capacity/CapacitySettingsPage';

export const metadata = {
  title: 'Capacity Settings – Department Head',
  description: 'Set the baseline daily capacity for your department.',
};

export default function CapacitySettingsRoute() {
  return <CapacitySettingsPage />;
}
